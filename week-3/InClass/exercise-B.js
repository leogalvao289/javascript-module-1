let classone = ['Leonardo', 'Maria Rita', 'yo', 'tu']
let classtwo = ['Leo', 'Mari', 'nosotros']
let name = 'antonio'

function name(miArray) { 
    if ([i].map().includes(name)){
    return name + "is not at the class with" + miArray;
} else {
    return name + "is at the class with" + [i];

}
}

console.log(name(classone.concat(classtwo).sort()))