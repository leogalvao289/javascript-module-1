// Declare your function here
function datos (name, msg, edad) {
  const greeting = name + msg + edad
  return greeting
}
const name = 'Leonardo '
const msg = 'Hello my name is '
const edad = 30
console.log(msg, name, "and I'm", edad, 'years old')
